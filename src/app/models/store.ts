import { Location } from "./location";

export class Store {
    public sotreId: string;
    public storeName: string;
    public district: string;
    public province: string;
    public location: Location
}